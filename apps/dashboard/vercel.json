{
  "buildCommand": "cd ../.. && npm run build --workspace=@donkey-ideas/financial-engine && cd apps/dashboard && next build",
  "framework": "nextjs",
  "crons": [
    { "path": "/api/cron/portfolio", "schedule": "0 6 * * *" }
  ]
}
