{
  "buildCommand": "cd ../packages/database && npx prisma generate && cd ../../apps/dashboard && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "rootDirectory": "apps/dashboard"
}
